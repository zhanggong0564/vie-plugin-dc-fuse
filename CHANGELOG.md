# Changelog

本插件变更记录遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)
规范，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

## [0.2.1] - 2026-09-10

### 变更

- 默认检测模型更新为 `det_yolo_v6.onnx`。
- 新请求从 `AICameraModel` 的产品类型 `AIParameterValue` 构建
  推理输入和数据回流目录，并保留 `modelParams.product_model`
  作为旧格式兼容入口。
- 请求阶段校验产品类型非空；允许 `AICameraModel` 中不同版本
  重复同一型号，多个不同型号需由旧参数明确消歧。

## [0.2.0] - 2026-09-08（Git tag: `v0.2.0`）

### 变更

- 单层型号改为按型号精确校验金属片数量，避免将 4 个和 6 个无条件视为等价。
- 新增五种双层型号规则，按整图统计铜排、螺钉、金属片和下横梁螺钉数量。
- 下横梁螺钉支持独立启用，复用现有检测类别且保持请求和响应契约不变。
- 修正螺钉、螺母和小螺钉判定：正常件数量必须精确匹配，且出现对应缺失标签时判定不通过。
- 上、下横梁螺钉改为独立判定，避免缺陷明细与整体结论不一致。
- 补充六种已注册型号、缺失标签冲突和横梁螺钉的回归测试。
- 请求参数与 AICameraModel 复用框架公共结构，保持原 JSON 契约不变。
- 场景运行配置迁移到框架 `SceneSettings`，支持 `DC_FUSE_` 环境变量前缀及阈值校验。

## [0.1.1] - 2026-07-28

### 变更

- 简化检测项数量判定与结果组装，保持各型号启用项和判定规则不变。
- 忽略本地运行日志目录，避免生成文件污染插件工作区。
- 场景注册迁移到框架 `ScenarioRegistry`。
- YOLO 检测器改为接收框架创建的推理 runner，模型路径不再由模型对象加载。
- 初始化失败时关闭已创建 runner，服务和示例退出时释放模型资源。
- 新增 runner 注入、初始化回滚和关闭契约测试。
- 同步示例和配置中的模型路径，采用 `weights/{scene}/{task}_{arch}_v{N}` 命名规范。

## [0.1.0] - 2026-05-30

### 新增

- 将直流熔丝旧场景迁移为 `vie-plugin-dc-fuse` 独立插件。
- 新增插件配置、路由、业务逻辑、单元测试和运行示例。
- 新增框架与插件二进制 wheel 构建配置。
